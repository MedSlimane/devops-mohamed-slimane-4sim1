apiVersion: v1
kind: Service
metadata:
  name: student-management
  namespace: student-management
spec:
  selector:
    app: student-management
  ports:
    - name: http
      port: 8089
      targetPort: 8089
  type: NodePort
